RSpec.describe Activerecord::CreateOrFindBy::Backport do
  it "has a version number" do
    expect(Activerecord::CreateOrFindBy::Backport::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
